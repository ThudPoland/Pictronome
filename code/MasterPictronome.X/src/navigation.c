#include "../include/navigation.h"

bool Navigate(struct MenuEntry *entry, ButtonFlags* flags)
{
    if(flags->button1Flag)
    {
        flags->button1Flag = 0;
    }
    else if(flags->button1Flag)
    {
        flags->button1Flag = 0;
    }
    else if(flags->button1Flag)
    {
        flags->button1Flag = 0;
    }
    else if(flags->button1Flag)
    {
        flags->button1Flag = 0;
    }
    else if(flags->button1Flag)
    {
        flags->button1Flag = 0;
    }
}
